config {
    shell "/bin/bash"
}

var {
    name "tim"

    GREEN {
        run { tput -Txterm setaf 2 }
    }
    YELLOW {
        run { tput -Txterm setaf 3 }
    }
}

target hellogoodbye {
    run { echo "${GREEN}hello $name" }
    run { echo "${YELLOW}goodbye $name" }
}

run hellogoodbye